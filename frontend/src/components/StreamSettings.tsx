const StreamSettings = () => {
    let receivedStreamName = '';
    let receivedStreamingKey = '';
  
    // Define a function to receive the values
    const receiveStreamInfo = (name: string, key: string) => {
      receivedStreamName = name;
      receivedStreamingKey = key;
    };
  
    // Other content of the StreamSettings component
  };
  
export default StreamSettings;
  