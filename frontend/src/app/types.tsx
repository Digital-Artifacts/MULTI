export type StreamPlatform = {
    streamTitle: string;
    streamUrl: string;
    streamKey: string;
    channelID: string;
  };

export type SettingsProps = {
    streamName: string;
    streamingKey: string;
  }


export type LivePageProps = {
    streamName: string;
    streamingKey: string;
}